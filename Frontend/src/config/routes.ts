type Routes = {
    home: string,
    cart: string,
    login: string,
    register: string,
    products: string,
    productDetail: string,
    profile: string,
    checkout: string,
    shop: string,
    features: string,
    blog: string,
    about: string,
    contact: string,
}

const routes: Routes = {
    home: '/',
    cart: '/cart',
    login: '/login',
    register: '/register',
    products: '/products',
    productDetail: '/product-detail',
    profile: '/profile',
    checkout: '/checkout',
    shop: '/shop',
    features: '/features',
    blog: '/blog',
    about: '/about',
    contact: '/contact',
}

export default routes;