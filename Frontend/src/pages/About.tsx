import { Header } from "../components/layout/Header";

export const About: React.FC = () => {
    return (
        <div>
            <div>
                <Header />
            </div>
            <section>

            </section>
        </div>
    );
}