type Routes = {
    home: string,
    cart: string,
    login: string,
    register: string,
    products: string,
    productDetail: string,
    profile: string,
    checkout: string,
}

const routes: Routes = {
    home: '/',
    cart: '/cart',
    login: '/login',
    register: '/register',
    products: '/products',
    productDetail: '/product-detail',
    profile: '/profile',
    checkout: '/checkout',
}

export default routes;